{{#unless hideSources}}
    {{> member.sources}}
{{/unless}}

{{> comment}}

{{#if typeParameters}}
    <h4 class="tsd-type-parameters-title">类型参数(范型)</h4>
    {{> typeParameters}}
{{/if}}

{{#if parameters}}
    <h4 class="tsd-parameters-title">参数</h4>
    <ul class="tsd-parameters">
        {{#each parameters}}
            <li>
                <h5>{{#compact}}
                    {{#each flags}}
                        <span class="tsd-flag ts-flag{{this}}">{{this}}</span>&nbsp;
                    {{/each}}
                    {{#if flags.isRest}}<span class="tsd-signature-symbol">...</span>{{/if}}
                    {{name}}:&nbsp;
                    {{#with type}}{{>type}}{{/with}}
                    {{#if defaultValue}}
                        <span class="tsd-signature-symbol">
                        &nbsp;=&nbsp;
                        {{defaultValue}}
                        </span>
                    {{/if}}
                {{/compact}}</h5>

                {{> comment}}

                {{#if type.declaration}}
                    {{#with type.declaration}}
                        {{> parameter}}
                    {{/with}}
                {{/if}}
            </li>
        {{/each}}
    </ul>
{{/if}}

{{#if type}}
    <h4 class="tsd-returns-title">返回 {{#compact}}{{#with type}}{{>type}}{{/with}}{{/compact}}</h4>

    {{#if comment.returns_i18n}}
        <div class="tsd_i18n">
            {{#markdown}}{{{comment.returns_i18n}}}{{/markdown}}
        </div>
    {{/if}}
    {{#if comment.returns}}
        <div class="tsd_en">
            {{#markdown}}{{{comment.returns}}}{{/markdown}}
        </div>
    {{/if}}

    {{#if type.declaration}}
        {{#with type.declaration}}
            {{> parameter}}
        {{/with}}
    {{/if}}
{{/if}}
